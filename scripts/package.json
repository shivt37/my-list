{
  "name": "my-list-scraper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub Actions scraper for my-list: mdblist.com listing URLs -> data/*.json + run records to worker KV.",
  "scripts": {
    "scrape": "node scrape.mjs"
  },
  "dependencies": {
    "puppeteer": "^25.3.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
